-- Configuration: https://github.com/nvim-treesitter/nvim-treesitter#available-modules
require "nvim-treesitter.configs".setup {
    ensure_installed = "maintained",
    highlight = {
        enable = true,
    }
}
